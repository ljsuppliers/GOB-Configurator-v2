rules_version = '2';
// INTERIM (import in progress) - replaced by staff-only rules in configurator/firestore.rules
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
